<UserControl x:Class="Compras.Views.ViewConsultaProdutos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Compras.Views"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid>
        <telerik:RadGridView ItemsSource="{Binding Descricoes}"
                             SelectedItem="{Binding Descricao}"
                             AutoGenerateColumns="False"
                             ShowGroupPanel="False"
                             IsFilteringAllowed="True">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding codcompladicional}" Header="COD." Width="90" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding familia}" Header="FAMÍLIA" Width="140" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding planilha}" Header="PLANILHA" Width="120" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding descricao_completa}" Header="DESCRIÇÃO" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding unidade}" Header="UNID." Width="90" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding inativo}" Header="ATIVO" Width="80" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding saldo_estoque}" Header="ESTOQUE" DataFormatString="{}{0:N2}" Width="100" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
