<UserControl x:Class="Compras.Views.PopUp.PopUpLocalizaProduto"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Compras.Views.PopUp"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <telerik:RadWatermarkTextBox x:Name="txtBusca"
                                     Margin="5"
                                     WatermarkContent="Buscar Produto" />

        <telerik:RadGridView x:Name="dataGrid"
                             Grid.Row="1"
                             ItemsSource="{Binding Descricoes}"
                             SelectedItem="{Binding Descricao}"
                             AutoGenerateColumns="False"
                             ShowGroupPanel="False"
                             FontSize="10"
                             IsFilteringAllowed="True"
                             MouseDoubleClick="dataGrid_MouseDoubleClick">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding planilha}"
                                            Header="PLANILHA"
                                            Width="130" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding descricao_completa}"
                                            Header="DESCRIÇÃO" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding unidade}"
                                            Header="UNID."
                                            Width="90" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
