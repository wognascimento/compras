<UserControl x:Class="Compras.Views.ViewCadastroFamiliaComprador"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Compras.Views"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="500"
             Loaded="UserControl_Loaded">
    <Grid>
        <telerik:RadGridView x:Name="familiasGrid"
                             ItemsSource="{Binding FamiliasProduto}"
                             SelectedItem="{Binding FamiliaProduto}"
                             AutoGenerateColumns="False"
                             ShowGroupPanel="False"
                             IsFilteringAllowed="True"
                             CanUserInsertRows="False"
                             RowEditEnded="FamiliasGrid_RowEditEnded">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn DataMemberBinding="{Binding codigofamilia}"
                                            Header="ID"
                                            IsReadOnly="True" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding nomefamilia}"
                                            Header="FAMÍLIA"
                                            IsReadOnly="True" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding res_compra}"
                                            Header="COMPRADOR(A)" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
