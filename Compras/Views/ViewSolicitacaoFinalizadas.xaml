<UserControl x:Class="Compras.Views.ViewSolicitacaoFinalizadas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Loaded="UserControl_Loaded">

    <Grid>
        <telerik:RadGridView x:Name="itensSolicitados"
                             ItemsSource="{Binding SolicitacoesEncaminhadas}"
                             SelectedItem="{Binding SolicitacaoEncaminhada}"
                             AutoGenerateColumns="False"
                             CanUserInsertRows="False"
                             ShowGroupPanel="False"
                             IsFilteringAllowed="True"
                             CanUserDeleteRows="False"
                             SelectionMode="Extended"
                             RowEditEnded="itensSolicitados_RowEditEnded">

            <telerik:RadGridView.Columns>
                <telerik:GridViewCheckBoxColumn
                    DataMemberBinding="{Binding finalizado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    EditTriggers="CellClick"
                    Header="FINALIZADO"
                    IsReadOnly="False"
                    Width="90" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding finalizado_por}" Header="FINALIZADO POR" Width="140" IsReadOnly="True" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding finalizado_em}" Header="FINALIZADO EM" DataFormatString="{}{0:dd/MM/yyyy HH:mm:ss}" Width="150" IsReadOnly="True" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding data_informado}" Header="DATA SOLICITADO" DataFormatString="{}{0:dd/MM/yyyy HH:mm:ss}" Width="150" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding familia}" Header="FAMÍLIA" Width="120" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding codcompleadicional}" Header="CÓD." Width="90" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding descricao_completa}" Header="DESCRIÇÃO" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding quantidade}" Header="QTDE." DataFormatString="{}{0:N2}" Width="90" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding quantidade_compra}" Header="QTDE COMPRA" DataFormatString="{}{0:N2}" Width="110" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding saldo_estoque}" Header="ESTOQUE" DataFormatString="{}{0:N2}" Width="100" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding unidade}" Header="UNID." Width="80" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding obs_solicitacao}" Header="OBS." Width="160" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding orientacao_compra}" Header="ORIENTAÇÃO COMPRA" Width="160" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding orientacao_roteiro}" Header="ORIENTAÇÃO ROTEIRO" Width="160" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding solicitante}" Header="SOLICITANTE" Width="130" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding cliente}" Header="CLIENTE" Width="130" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding data_utilizacao}" Header="UTILIZAÇÃO" DataFormatString="{}{0:dd/MM/yyyy}" Width="110" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding planilha}" Header="PLANILHA" Width="110" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding resp_compra}" Header="COMPRADORA(O)" Width="130" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding cod_solicitacao}" Header="SOLICITAÇÃO" Width="110" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding tipo}" Header="TIPO" Width="100" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding data_entrega}" Header="ENTREGA" DataFormatString="{}{0:dd/MM/yyyy}" Width="110" />
                <telerik:GridViewDataColumn DataMemberBinding="{Binding username}" Header="INSERIDO POR" Width="120" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>

</UserControl>
